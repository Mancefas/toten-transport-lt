'use client';

import { createTheme } from '@mantine/core';

export const theme = createTheme({
  /* Put your mantine theme override here */
  shadows: {
    custom: '0px 0px 15px 0px rgba(148,142,148,0.2)',
  },
});
